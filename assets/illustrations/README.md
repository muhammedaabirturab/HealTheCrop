# Illustrations

Space for onboarding/empty-state illustrations (e.g. "no devices connected yet",
"no scan history"). None are bundled by default — the app uses icon + copy fallbacks
instead (see `frontend/src/pages/Dashboard.tsx`'s empty-device state, for example).

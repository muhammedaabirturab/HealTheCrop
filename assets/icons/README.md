# Icons

The live app uses [lucide-react](https://lucide.dev) for all UI icons (see
`frontend/src/components/`), so no custom icon files are required to run it. Drop
brand-specific SVG/PNG icons here if you want to replace any of them.
